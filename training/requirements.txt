# Train YOLO26 detect on Intel Arc (PyTorch XPU) via the project .venv:
#   python training/train.py --size n
# Install/refresh XPU torch if needed:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/xpu
ultralytics>=8.4.0
torch
torchvision
PyYAML
numpy
opencv-python
